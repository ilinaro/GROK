import { useForm } from 'react-hook-form';
import styles from './FormInput.module.css';
import { FormInput } from '../modules/ui/FormInput/FormInput';
import { LoginForm } from '../modules/LoginForm';

export const LoginPage = () => {
  return <LoginForm />;
};
